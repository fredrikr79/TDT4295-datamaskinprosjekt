// Includes
#include "myMain.h"
#include "main.h" 

void myMain(void) {

  /* Infinite loop */
  while (1) {
    HAL_GPIO_TogglePin(GPIOA, GPIO_PIN_5);
    HAL_Delay(500);



   // FPGA comm sate machine
   
   
   // HID input state machine


   //
  }
}
